# 依赖
node_modules
.pnpm-store

# 构建产物
dist
dist-ssr
*.local

# Tauri / Rust
src-tauri/target
src-tauri/gen
# 注意：Cargo.lock 必须提交。这是应用 crate（bin + cdylib），
# 按 Cargo 官方规范必须纳入版本控制，保证构建可复现、依赖可审计。

# Vite
.vite

# 编辑器
.vscode/*
!.vscode/extensions.json
.idea
*.swp
.DS_Store
Thumbs.db

# 系统
*.log
*.tsbuildinfo
公众号文章.md

# ZCode agent 工作目录
.zcode/

# 本地测试文档（不入版本控制）
图片渲染测试.md
